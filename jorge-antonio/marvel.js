#!/usr/bin/env node

const http = require('http');

function createUrl(dateDescriptor, ts, apiKey, hash)
{

}

function main()
{
   console.log('bar');
}

if (!module.parent)
   main();

module.exports = {createUrl};
